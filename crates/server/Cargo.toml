[package]
name = "wasi-faas-server"
version = "0.1.0"
edition = "2021"

[dependencies]
hyper = { version = "0.14.20", features = ["server", "http1", "tcp", "stream"] }
wasi-faas-runtime = { path = "../runtime", version = "0.1.0" }
wasi-faas-types = { path = "../types", version = "0.1.0" }
